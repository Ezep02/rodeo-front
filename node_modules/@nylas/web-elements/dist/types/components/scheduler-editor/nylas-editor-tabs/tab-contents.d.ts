export declare enum Tab {
    EventInfo = "eventInfo",
    Availability = "availability",
    BookingOptions = "bookingOptions",
    Communications = "communications",
    Participants = "participants",
    BookingForm = "bookingForm",
    PageStyles = "pageStyles"
}
export type TabContentsProps = {
    id: string;
    formState: any;
    activeTab: Tab;
    calendars?: any[];
    currentUser?: any;
    i18next: any;
};
export default function TabContents({ id, formState, activeTab, i18next }: TabContentsProps, children: any): any;
