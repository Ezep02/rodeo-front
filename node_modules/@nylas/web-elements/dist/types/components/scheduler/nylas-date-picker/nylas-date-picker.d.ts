import { EventEmitter } from '../../../stencil-public-runtime';
import { ConfigSettings } from "../../../stores/scheduler-store";
import { ThemeConfig } from '@nylas/core';
export declare class NylasDatePicker {
    private host;
    selectableDates?: Date[];
    configSettings?: ConfigSettings;
    readonly themeConfig?: any;
    selectedDate?: Date;
    selectedLanguage?: string;
    readonly isLoading?: boolean;
    readonly eventDuration?: number;
    month: Date;
    dates: Date[];
    disableNextMonthButton: boolean;
    dateSelected: EventEmitter<Date>;
    monthChanged: EventEmitter<Date>;
    configSettingsChanged(newConfigSettings: ConfigSettings): void;
    selectedLanguageChanged(newLanguage: string): void;
    themeConfigChanged(newThemeConfig: ThemeConfig): void;
    connectedCallback(): void;
    disconnectedCallback(): void;
    componentWillLoad(): void;
    componentDidLoad(): void;
    applyThemeConfig(themeConfig?: ThemeConfig): void;
    private getDates;
    private selectDate;
    private changeMonth;
    private isSelected;
    private isDisabled;
    render(): any;
}
