import { EventEmitter } from '../../../stencil-public-runtime';
import { AvailabilityTimeslot } from "../../../stores/scheduler-store";
import type { ThemeConfig, Timeslot } from '@nylas/core';
export declare class NylasTimeslotPicker {
    private host;
    readonly availability?: AvailabilityTimeslot[];
    readonly isLoading?: boolean;
    readonly themeConfig?: any;
    readonly selectedTimeslot?: Timeslot;
    readonly selectedTimezone: string;
    readonly selectedDate?: Date;
    selectedLanguage?: string;
    readonly timeslotSelected: EventEmitter<Timeslot>;
    readonly timeslotConfirmed: EventEmitter<Timeslot>;
    times: AvailabilityTimeslot[];
    hoveredTimeslotIndex?: number;
    selectedTimeslotIndex?: string;
    selectedLanguageChanged(newLanguage: string): void;
    selectedDateChanged(newVal: Date | null | undefined): void;
    availabilityChanged(newVal: AvailabilityTimeslot[] | undefined): void;
    themeConfigChanged(newThemeConfig: ThemeConfig): void;
    connectedCallback(): void;
    disconnectedCallback(): void;
    componentWillLoad(): Promise<void>;
    componentDidLoad(): Promise<void>;
    applyThemeConfig(themeConfig?: ThemeConfig): void;
    private getTimeslotId;
    private onClickSelectTime;
    private handleConfirmedTimeslot;
    private handleMouseEnter;
    private handleMouseLeave;
    private getTimeSlotLabel;
    render(): any;
}
