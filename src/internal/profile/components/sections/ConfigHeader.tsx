import React from "react";
import { ConfigViewMode } from "../../types/ConfigViewMode";

const ConfigHeader = () => {
  const [view, setView] = React.useState<ConfigViewMode>("Perfil");

  return (
    <section>

    </section>
  );
};

export default ConfigHeader;
